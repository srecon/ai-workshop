# Corporate Components

This directory is a catalog of approved corporate components and implementation signals.

It is not a source of compliance decisions. Compliance decisions are defined only in `rules/PR-*.yaml`.

`standin.yaml` is intentionally kept as a corporate component, but `compliance_enabled: false`: StandIn is not a standalone Platform-Ready 5.3.1 requirement.
